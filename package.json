{
  "name": "antifantwerp",
  "version": "1.0.0",
  "description": "Website for antifantwerp",
  "main": "index.js",
  "repository": "https://github.com/Antifantwerp/antifantwerp.github.io.git",
  "author": "Antifantwerp <antifantwerp@proton.me>",
  "license": "MIT",
  "scripts": {
    "dev": "yarn eleventy --serve",
    "build": "yarn eleventy",
    "cms:pull": "directus-to-data --output-filename src/_data/{{collectionName}}.json -c AFAOrganisations -c AFAStickers",
    "cms:schema:backup": "directus-to-data --backup-schema .directus.schema.json -c AFAOrganisations -c AFAStickers",
    "cms:schema:restore": "directus-to-data --restore-schema .directus.schema.json -c AFAOrganisations -c AFAStickers",
    "chms:schema:apply": "directus-to-data --restore-schema .directus.schema.json --apply-schema -c AFAOrganisations -c AFAStickers"
  },
  "devDependencies": {
    "@11ty/eleventy": "^2.0.1",
    "directus-to-data": "^0.6.1",
    "eleventy-sass": "^2.2.3",
    "favicons": "^7.1.4",
    "js-yaml": "^4.1.0",
    "jstransformer-markdown-it": "^3.0.0",
    "sass": "^1.69.4"
  }
}
