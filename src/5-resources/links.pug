
each links in resources.links
    h2=links.title
    if links.subtitle
        p=links.subtitle
    ul
        each entry in links.entries
            li
                a(href=entry.url)=entry.name
